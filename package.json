{
  "name": "reactiv",
  "version": "0.3.1",
  "description": "A react-like incremental vdom renderer",
  "main": "index.js",
  "scripts": {
    "test": "webpack --config webpack.test.config.js && karma start --single-run && npm build && npm install -g . && npm s -g && node test-require.js",
    "build": "webpack && tsc -d -p . && del reactiv.js",
    "publish_patch": "npm run build && npm version patch && npm publish",
    "publish_minor": "npm run build && npm version minor && npm publish"
  },
  "keywords": [
    "react",
    "jsx",
    "incremental",
    "virtual",
    "dom"
  ],
  "author": "jeremy kothe",
  "license": "ISC",
  "devDependencies": {
    "awesome-typescript-loader": "^0.15.9",
    "jasmine": "^2.4.1",
    "typescript": "^1.7.5"
  }
}
