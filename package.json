{
  "name": "reactiv",
  "version": "0.3.2",
  "description": "A react-like incremental vdom renderer",
  "main": "./dist/reactiv.js",
  "scripts": {
    "int": "tslint reactiv.ts",
    "build": "tsc -d -p . --outDir dist && webpack",
    "test": "webpack --config webpack.test.config.js && karma start --single-run && npm run build && npm install -g . && npm ls -g && cd test && node test-require.js && cd .. && npm remove -g .",
    "publish_patch": "npm run build && npm version patch && npm publish",
    "publish_minor": "npm run build && npm version minor && npm publish"
  },
  "keywords": [
    "react",
    "jsx",
    "incremental",
    "virtual",
    "dom"
  ],
  "author": "jeremy kothe",
  "license": "ISC",
  "devDependencies": {
    "awesome-typescript-loader": "^0.15.9",
    "jasmine-core": "^2.4.1",
    "karma": "^0.13.22",
    "karma-chrome-launcher": "^0.2.2",
    "karma-jasmine": "^0.3.8",
    "karma-phantomjs-launcher": "^1.0.0",
    "phantomjs-prebuilt": "^2.1.5",
    "typescript": "^1.7.5",
    "webpack": "^1.12.14"
  }
}
